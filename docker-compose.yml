version: "3.9"

services:
  app:
    build:
      context: ./docker/php
    ports:
      - "8081:8081"
    volumes:
      - .:/var/www/html
    networks:
      - laravel

  nginx:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - laravel

  db:
    image: postgres:15
    container_name: laravel_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: laravel
      POSTGRES_USER: laravel
      POSTGRES_PASSWORD: laravel
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - laravel

  faceid-service:
    build:
      context: ./faceid-service
      dockerfile: Dockerfile
    container_name: faceid-service
    volumes:
      - ./faceid-service:/app
    privileged: true
    devices:
      - "/dev/video0:/dev/video0" # OBLIGATORIO para que Python vea la cámara
    networks:
      - laravel

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"     # Puerto AMQP (Laravel, Python, MCP)
      - "15672:15672"   # Panel de administración
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - laravel


volumes:
  postgres_data:

networks:
  laravel:
    driver: bridge
